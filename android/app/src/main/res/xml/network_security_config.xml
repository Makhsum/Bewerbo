<?xml version="1.0" encoding="utf-8"?>
<!--
  Plain HTTP is allowed only to the addresses a development backend runs on: 10.0.2.2 is the host
  machine as the emulator sees it. Everything else stays HTTPS-only, which matters because the
  payload here is somebody's CV.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
